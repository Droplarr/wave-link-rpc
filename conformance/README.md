# Conformance fixtures

These language-neutral JSON cases describe redacted protocol inputs and expected
normalized outcomes. Fixtures must use synthetic identifiers and must never
contain usernames, application/device names, icons, images, or other
machine-specific data.

